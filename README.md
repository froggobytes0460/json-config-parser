# JSON Configuration Parser

## Overview

This project takes in a JSON file, validate it against a known schema (e.g. a set of expected keys and types), deserialize it into typed C++ structs, and let users query or pretty-print it from the CLI.
